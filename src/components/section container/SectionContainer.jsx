

const SectionContainer = ({ children }) => {
    return (
        <div className="max-w-7xl mx-auto lg:px-10 md:px-12 px-3">
            {children}
        </div>
    );
};

export default SectionContainer;